# دليل الاستخدام الشامل — Cyber Guardians Mobile

> لعبة تعليمية تفاعلية ثلاثية الأبعاد لتعليم أساسيات الأمن السيبراني للمراهقين
> الإصدار: **11.0.0** (نظام الأدوات المتعدد)

---

## فهرس المحتوى

1. [مقدمة عامة](#1-مقدمة-عامة)
2. [دليل الاستخدام الداخلي](#2-دليل-الاستخدام-الداخلي-جديد)
3. [لوحة الذكاء الاصطناعي](#3-لوحة-الذكاء-الاصطناعي)
4. [الجهة المحلية (Local Agent)](#4-الجهة-المحلية-local-agent)
5. [نصائح وأفضل الممارسات](#5-نصائح-وأفضل-الممارسات)

---

## 1. مقدمة عامة

### ما هي اللعبة؟

Cyber Guardians Mobile (حراس الأمن السيبراني) هي لعبة تعليمية تفاعلية ثلاثية الأبعاد مصممة خصيصاً للمراهقين لتعلم أساسيات الأمن السيبراني بطريقة ممتعة. تشمل اللعبة:

- **7 مستويات تعليمية** تغطي مواضيع: التصيد الاحتيالي، كلمات المرور، البرمجيات الخبيثة، أمن الشبكات، التشفير، أمن الويب، والاستجابة للحوادث
- **5 شخصيات تعليمية**: زين (محلل أمني)، د. نورا (خبيرة تشفير)، عمر (خبير شبكات)، ليلى (خبيرة أمن ويب)، طارق (محلل برمجيات خبيثة)
- **نظام Gamification شامل**: نقاط خبرة (XP)، رتب، شارات، مكافآت يومية، مهام، كومبو، اختبارات بمستويات صعوبة مختلفة
- **لوحة ذكاء اصطناعي متقدمة** مع 6 تبويبات رئيسية وعشرات الميزات

### كيف تبدأ؟

1. افتح اللعبة في المتصفح على الرابط: `https://cyber-guardians-mobile.pages.dev`
2. يمكنك تثبيت اللعبة كتطبيق (PWA) عبر خيار "تثبيت التطبيق" في المتصفح
3. أدخل اسمك في خانة اسم اللاعب
4. اضغط **"ابدأ المغامرة"** من القائمة الرئيسية
5. اختر المستوى الأول "رسالة مشبوهة" وابدأ التعلم

### اختصارات لوحة المفاتيح

| المفتاح | الوظيفة |
|---------|---------|
| `M` | كتم/تشغيل الصوت |
| `B` | كتم/تشغيل موسيقى الخلفية |
| `Esc` | العودة للشاشة السابقة |

---

## 2. دليل الاستخدام الداخلي (جديد)

### ما هو الدليل الداخلي؟

الدليل الداخلي هو شاشة تعليمية تفاعلية تظهر داخل اللعبة مباشرة لتشرح لك كيفية استخدام جميع الميزات. وهو متوفر باللغة العربية بالكامل.

### كيف يعمل؟

- **عند أول زيارة**: يظهر الدليل تلقائياً عند فتح القائمة الرئيسية لأول مرة
- **من القائمة الرئيسية**: اضغط على زر **❓** في الشريط السفلي لفتح الدليل في أي وقت
- **من الإعدادات**: يمكنك تفعيل/تعطيل الدليل من إعدادات الواجهة

### أقسام الدليل (11 قسماً)

| # | القسم | المحتوى |
|---|-------|---------|
| 1 | 🛡️ مرحباً بك | مقدمة عن اللعبة وأهدافها |
| 2 | 🏠 القائمة الرئيسية | شرح جميع أزرار القائمة |
| 3 | 🎮 المستويات والتحديات | 7 مستويات و7 أنواع تحديات |
| 4 | ⭐ نظام التقدم والمكافآت | XP، رتب، شارات، كومبو، أرواح |
| 5 | 🤖 مساعد الذكاء الاصطناعي | فتح واستخدام لوحة AI |
| 6 | 🛠️ أدوات AI المتقدمة | 13 تبويب فرعي للأدوات |
| 7 | 🤖 الوكيل المحلي | Local Agent والإضافات |
| 8 | ⚙️ الإعدادات والتخصيص | ثيمات، خطوط، صوت، أمان |
| 9 | 👨‍🏫 وضع هيئة التدريس | أداة المعلمين |
| 10 | ⌨️ اختصارات لوحة المفاتيح | M، B، Escape |
| 11 | 💡 نصائح مهمة | نصائح للتقدم السريع |

### التحكم في الدليل

- اضغط **التالي** للانتقال للقسم التالي
- اضغط **السابق** للعودة للقسم السابق
- اضغط **تخطي** لإغلاق الدليل
- اضغط **ابدأ المغامرة!** في القسم الأخير لإغلاق الدليل والبدء باللعبة
- شريط التقدم في الأعلى يُظهر تقدمك عبر الأقسام

---

## 2. لوحة الذكاء الاصطناعي

### فتح لوحة AI

- اضغط على زر **🤖** العائم في الزاوية اليمنى العليا من الشاشة
- الزر قابل للسحب بإعادة وضعه في أي مكان على الشاشة
- عند المرور بالزر تظهر تلميح "AI & Advanced Settings"
- للإغلاق: اضغط زر **✕** أو النافذة المعتمة أو زر AI مرة أخرى

### التبويبات الستة الرئيسية

اللوحة تحتوي على 6 تبويبات رئيسية في الشريط العلوي:

| التبويب | الرمز | الوظيفة |
|---------|-------|---------|
| **الطالب** | 💬 | محادثة AI تعليمية |
| **هيئة التدريس** | 🎓 | تعديل المحتوى عبر AI |
| **🛠️ أدوات** | 🔧 | 13 تبويب فرعي |
| **📁 مشروع** | 📂 | معرفة، تعليمات، محادثات |
| **⚙** | ⚙️ | إعدادات المزود والنموذج |
| **🎨** | 🎨 | إعدادات المظهر والواجهة |

---

### تبويب الطالب 💬

تبويب المحادثة الرئيسي للطلاب. يمكنه الإجابة عن أي موضوع: أمن سيبراني، علوم، تكنولوجيا، تاريخ، ثقافة، برمجة، وغيرها.

#### كيف ت问询 AI عن أي موضوع

1. افتح لوحة AI واضغط تبويب **💬 الطالب**
2. اكتب سؤالك في خانة الإدخال في الأسفل
3. اضغط زر **إرسال** أو اضغط `Enter`
4. سيظهر الرد مباشرة مع إمكانية النسخ أو التنزيل

**أمثلة على الأسئلة:**
- "ما هو التصيد الاحتيالي؟"
- "كيف أبني كلمة مرور قوية؟"
- "اشرح لي كيف يعمل التشفير"
- "ما الفرق بين Firewall و VPN؟"

#### إنشاء جلسات محادثة متعددة

- في أعلى شاشة المحادثة، توجد شريط جلسات
- اضغط **+ جلسة** لإنشاء جلسة جديدة
- اضغط مرتين على اسم الجلسة لتغيير اسمها
- اضغط **✕** بجانب الجلسة لحذفها
- اضغط على أي جلسة للتبديل إليها

#### كيف تفعّل مهارة (Skills)

1. من تبويب **🛠️ أدوات** ← **📋 قدرات**
2. فعّل المهارة المطلوبة بالضغط على زر التبديل
3. اختر المهارة النشطة بالضغط على "تفعيل"
4. اطلب من AI استخدام المهارة في المحادثة

**أو** قل في المحادثة مباشرة: "استخدم قدرة [اسم القدرة]"

**أنواع القدرات المتاحة:**
- 🌐 ترجمة، 🔍 تحليل، 📝 كتابة، 🧮 رياضيات، 💻 برمجة، 🔬 بحث، 🎨 إبداع، ⚙️ مخصص

#### كيف تستخدم Deepthink للتفكير العميق

1. في شاشة المحادثة، اضغط زر **🧠** بجانب زر الإرسال
2. سيتحول اللون إلى البنفسجي عند التفعيل
3. اكتب سؤالك المركّب
4. سيقوم AI بـ 3 خطوات: **التفكير** ← **المراجعة** ← **الإجابة**

**متى تستخدمه:**
- أسئلة تحتاج تحليل عميق
- مشاكل تحتاج حلولاً متعددة
- أسئلة تعليمية معقدة

#### كيف تولّد صوراً عبر Pollinations

1. اكتب في المحادثة: "ولّد صورة لـ [وصف الصورة]"
2. أو "توليد صورة: [الوصف]"
3. سيستخدم AI أداة image_generator عبر Pollinations.ai
4. ستظهر الصورة مُرفقة في المحادثة

**أمثلة:**
- "ولّد صورة لروبوت يحرس شبكة حاسوب"
- "توليد صورة: خبير أمن سيبراني يفحص كود"

#### البحث في الويب

- اضغط زر **🔍** بجانب زر الإرسال لتفعيل/تعطيل البحث
- عند التفعيل، يبحث AI في الويب قبل الإجابة
- يبحث في: Skills، Plugins، Knowledge، والويب

#### رفع الملفات

- اضغط زر **📎** لرفع ملف
- يدعم: نصوص، صور، فيديو، صوت، JSON
- الصور: تُعرض مباشرة في المحادثة
- الفيديو: يستخرج الإطارات الرئيسية
- الصوت: يحلل المدة والقنوات ومستوى الصوت

#### تنزيل الردود

- اضغط زر **⬇️** أسفل أي رد AI
- الخيارات: `.md` (Markdown)، `.docx` (Word)، `.html` (صفحة ويب)

---

### تبويب هيئة التدريس 🎓

مخصص للمعلمين ومطوري المحتوى لتعديل وتطوير اللعبة عبر المحادثة.

#### كيف تعدل المحتوى عبر AI

1. افتح تبويب **🎓 هيئة التدريس**
2. اكتب طلب التعديل مباشرة، مثال:
   - "غيّر عنوان المستوى الأول إلى 'رسالة خداعية'"
   - "أضف شخصية جديدة اسمها 'سارة'"
   - "احذف المستوى السابع"
3. سيقوم AI بتوليد كود JSON والتطبيق تلقائياً

**أنواع التعديلات المدعومة:**

| النوع | المثال |
|-------|--------|
| تعديل إعدادات اللعبة | `غيّر عنوان اللعبة` |
| تعديل مستوى | `عدّل نقاط المستوى 3 إلى 200` |
| إضافة مستوى | `أضف مستوى جديد عن التشفير` |
| حذف مستوى | `احذف المستوى 5` |
| تعديل شخصية | `غيّر لون زين إلى أخضر` |
| إضافة شخصية | `أضف شخصية جديدة` |
| تعديل ملف مصدر | `عدّل ملف badges.ts` |

#### كيف تنشئ مستويات جديدة

1. اكتب في المحادثة:
   ```
   أضف مستوى جديد:
   - العنوان: [عنوان المستوى]
   - النوع: [cards/build/maze/dragdrop/decrypt/codefix/response]
   - الصعوبة: [easy/medium/hard]
   - النقاط: [رقم]
   - الشخصية الرئيسية: [zayn/nora/omar/layla/tariq]
   ```
2. سيقوم AI بتوليد JSON وإضافته تلقائياً

#### كيف تعدل الشخصيات والحوار

1. اكتب طلباً مثل:
   - "غيّر حوار المستوى الأول — اجعل زين يقول: [النص الجديد]"
   - "عدّل شخصية ليلى: غيّر الدور إلى 'خبيرة أمن سحابي'"
2. سيقوم AI بتطبيق التعديلات

**الأسماء المدعومة للشخصيات:** `zayn`, `nora`, `omar`, `layla`, `tariq`, `system`

#### تعديل الملفات مباشرة

1. في تبويب **📁 ملفات** داخل هيئة التدريس
2. اختر الملف من القائمة
3. عدّل المحتوى يدوياً في المحرر
4. اضغط **حفظ**

**الملفات المتاحة للتعديل:**
- `src/data/gameMeta.ts` — إعدادات اللعبة
- `src/data/characters.ts` — الشخصيات
- `src/data/dialogue.ts` — الحوار والمستويات
- `src/data/ranks.ts` — رتب اللاعبين
- `src/data/badges.ts` — الشارات
- `src/data/missions.ts` — المهام اليومية
- `src/data/quizQuestions.ts` — أسئلة الاختبار
- `src/data/assessmentQuestions.ts` — أسئلة التقييم
- `src/data/referenceContent.ts` — المرجع الأمني
- `src/data/challengeMeta.ts` — معلومات التحديات

#### رفع التعديلات إلى GitHub

1. اضغط **🔄 رفع إلى GitHub** في تبويب هيئة التدريس
2. يجب إعداد GitHub Token أولاً (من إعدادات GitHub)
3. الخيارات:
   - **التعديل المباشر** — يعدل في مستودعك الرئيسي
   - **إنشاء مستودع جديد** — ينسخ كل الملفات
   - **مزامنة مع مستودع موجود** — يُحدّث الملفات

#### الرفع التلقائي

- فعّل خيار **"🔄 رفع تلقائي عند التعديل"** في إعدادات هيئة التدريس
- سيتم رفع كل تعديل فوراً إلى GitHub

---

### تبويب 🛠️ أدوات (13 تبويب فرعي)

#### 1. قدرات (Skills) 📋

إدارة القدرات المخصصة التي يستخدمها AI في إجاباته.

##### إضافة قدرة جديدة

1. اضغط **+ قدرة جديدة**
2. اختر من القالب الجاهز أو أنشئ مخصصاً:
   - **الاسم**: اسم القدرة (مثال: "محلل أمني")
   - **الوصف**: وصف مختصر
   - **الأيقونة**: رمز للقدرة
   - **الفئة**: ترجمة/تحليل/كتابة/رياضيات/برمجة/بحث/إبداع/مخصص
   - **System Prompt**: تعليمات AI عند استخدام هذه القدرة
3. اضغط **حفظ**

##### تعديل قدرة

1. اضغط **✏️** بجانب القدرة
2. عدّل الحقول المطلوبة
3. اضغط **حفظ**

##### حذف قدرة

1. اضغط **🗑️** بجانب القدرة
2. اضغط **تأكيد الحذف**

##### إعادة ترتيب القدرات

- اسحب القدرة وأفلتها للأسفل أو الأعلى لإعادة ترتيب القائمة

##### تفعيل/تعطيل قدرة

- اضغط على زر التبديل بجانب القدرة
- عند التفعيل، يظهر اللون الأخضر

##### استيراد/تصدير

- **تصدير**: يُصدّر جميع القدرات كملف JSON
- **استيراد**: يُستورد قدرات من ملف JSON

---

#### 2. أدوات (Plugins) 🔌

إدارة الأدوات الخارجية التي يمكنها تنفيذ مهام محددة.

##### إضافة أداة

1. اضغط **+ أداة جديدة**
2. اختر من القالب أو أنشئ مخصصاً:
   - **الاسم**: اسم الأداة
   - **الفئة**: حاسبة/قاعدة بيانات/تصور/مسح ويب/ملفات/API/مخصص
   - **Base URL**: رابط API الأساسي
   - **نوع المصادقة**: لا شيء/مفتاح API/OAuth
3. اضغط **حفظ**

##### تنفيذ أداة

1. اختر الأداة من القائمة
2. اختر الـ **Endpoint** المطلوب من القائمة المنسدلة
3. أدخل المعاملات في الخانات المتاحة
4. اضغط **▶ تنفيذ**
5. ستظهر النتيجة في الأسفل

##### الأدوات الجاهزة المتاحة

| الأداة | الوظيفة |
|--------|---------|
| 🧮 حاسبة | حسابات رياضية |
| 🖼️ مولّد صور | توليد صور عبر Pollinations |
| 📊 مولّد رسوم بيانية | إنشاء رسوم بيانية |
| 🔍 محرك بحث | بحث في الويب |

---

#### 3. اتصالات (Connectors) 🔗

إدارة اتصالات مزودي AI المختلفين.

##### إضافة اتصال

1. اضغط **+ اتصال جديد**
2. اختر المزود من القائمة:
   - OpenAI، Anthropic، Google، Meta، Mistral
   - GitHub Copilot، Cursor، Codeium
   - AWS Bedrock، Azure OpenAI، Google Cloud
   - Ollama، LM Studio، مخصص
3. أدخل البيانات المطلوبة:
   - **Base URL**: رابط API
   - **API Key**: مفتاح API
   - **النماذج**: قائمة النماذج المدعومة (مفصولة بفاصلة)
4. اضغط **حفظ**

##### اختبار الاتصال

1. اضغط **🔌 اختبار** بجانب الاتصال
2. في حالة النجاح: تظهر رسالة ✅
3. في حالة الفشل: تظهر رسالة خطأ مع التفاصيل

##### تفعيل/تعطيل اتصال

- اضغط زر التبديل بجانب الاتصال
- عند قطع الاتصال، تُمسح بيانات الاعتماد تلقائياً

---

#### 4. سوق (Marketplace) 🛒

تصفح وتثبيت قوالب جاهزة من 35 قالب.

##### تصفح القوالب

1. افتح تبويب **🛒 سوق**
2. استخدم الألسنة: قدرات / أدوات / اتصالات
3. استخدم شريط البحث للبحث عن قالب محدد
4. استخدم الفلتر لتصفيته حسب الفئة

##### البحث والفلترة

- **شريط البحث**: اكتب اسم القالب أو كلمة مفتاحية
- **الفلتر**: اختر الفئة من القائمة المنسدلة
- **الترتيب**: بالاسم أو بالفئة

##### التثبيت

1. اضغط على القالب المطلوب
2. اضغط **تثبيت**
3. سيظهر القالب في تبويب القدرات/الأدوات/الاتصالات المناسب

##### الإزالة

1. اضغط على القالب المثبّت
2. اضغط **إزالة**

---

#### 5. وكيل (Local Agent) 🤖

> ⚠️ **مهم جداً:** يجب تشغيل جميع أوامر الجهة المحلية من مجلد الجهة فقط!

##### المتطلبات

- Node.js 18+ مُثبّت على جهازك
- Terminal مفتوح

##### تثبيت الجهة المحلية

```bash
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 📍 أولاً: انتقل إلى مجلد الجهة المحلية
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
cd cyber-guardians-mobile/cyberguard-agent

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 📦 ثانياً: ثبّت التبعيات
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
npm install
```

##### بدء التشغيل

```bash
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 📍 تأكد أنك في مجلد الجهة المحلية!
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
cd cyber-guardians-mobile/cyberguard-agent

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# ▶️ شغّل الجهة
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
npm start
```

**خيارات إضافية:**

| الخيار | الوصف | القيمة الافتراضية |
|--------|-------|-------------------|
| `--port` | رقم البورت | 3001 |
| `--profile` | ملف الإعدادات (minimal/full/education) | minimal |
| `--token` | رمز المصادقة | اختياري (فارغ by default) |

##### إعداد مفاتيح AI (اختياري)

```bash
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 📍 من مجلد الجهة المحلية فقط!
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
cd cyber-guardians-mobile/cyberguard-agent

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🔑 احصل على مفتاح API (اختر واحداً)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# --- Gemini (مجاني - Google AI Studio) ---
# 1. افتح: https://aistudio.google.com/apikey
# 2. اضغط "Create API Key"
# 3. انسخ المفتاح
export GEMINI_API_KEY="AIzaSy..."

# --- Groq (مجاني - سريع جداً) ---
# 1. افتح: https://console.groq.com/keys
# 2. اضغط "Create API Key"
# 3. انسخ المفتاح
export GROQ_API_KEY="gsk_..."

# --- HuggingFace (مجاني) ---
# 1. افتح: https://huggingface.co/settings/tokens
# 2. اضغط "New token"
# 3. انسخ التوكن
export HUGGINGFACE_API_KEY="hf_..."

# --- OpenRouter (مجاني) ---
# 1. افتح: https://openrouter.ai/keys
# 2. اضغط "Create Key"
# 3. انسخ المفتاح
export OPENROUTER_API_KEY="sk-or-..."

# --- Ollama (مجاني - محلي) ---
# لا يحتاج مفتاح! يجب تثبيته أولاً:
curl -fsSL https://ollama.com/install.sh | sh
ollama pull llama3.2
ollama serve

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# ▶️ شغّل الجهة بعد إعداد المفتاح
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
npm start
```

> 💡 **نصيحة:** لجعل المفاتيح تدوم بعد إغلاق Terminal، أضفها إلى `~/.bashrc` أو `~/.zshrc`:
> ```bash
> echo 'export GEMINI_API_KEY="your-key"' >> ~/.bashrc
> source ~/.bashrc
> ```

##### إضافة نماذج جديدة

```bash
# الملف: src/ai/providers.ts

# --- Gemini (سطر 31) ---
models = ['gemini-3.5-flash', 'gemini-3.1-flash-lite', 'gemini-3-flash']

# --- Groq (سطر 144) ---
models = ['llama3-70b-8192', 'llama3-8b-8192', 'mixtral-8x7b-32768', 'gemma-7b-it']

# --- HuggingFace (سطر 197) ---
models = ['meta-llama/Llama-3-70B-Instruct', 'mistralai/Mixtral-8x7B-Instruct-v0.1', 'google/gemma-7b-it']

# --- OpenRouter (سطر 255) ---
models = ['meta-llama/llama-3-70b-instruct', 'mistralai/mixtral-8x7b-instruct', 'google/gemma-7b-it:free']
```

**مثال — إضافة نموذج جديد لـ Groq:**

```typescript
// غيّر سطر 144 من:
models = ['llama3-70b-8192', 'llama3-8b-8192', 'mixtral-8x7b-32768', 'gemma-7b-it']

// إلى:
models = ['llama3-70b-8192', 'llama3-8b-8192', 'mixtral-8x7b-32768', 'gemma-7b-it', 'llama-3.3-70b-versatile']
```

##### إضافة مزود جديد بالكامل

**الخطوة 1: أضف كلاس في `src/ai/providers.ts`:**

```typescript
export class MyNewProvider implements AIProvider {
  name = 'mynewprovider'
  type = 'cloud' as const
  models = ['model-1', 'model-2']
  private apiKey: string
  private baseUrl = 'https://api.mynewprovider.com/v1'

  constructor(apiKey: string) {
    this.apiKey = apiKey
  }

  async isAvailable(): Promise<boolean> {
    try {
      const res = await fetch(`${this.baseUrl}/models`, {
        headers: { 'Authorization': `Bearer ${this.apiKey}` }
      })
      return res.ok
    } catch {
      return false
    }
  }

  async chat(model: string, messages: AIMessage[], options?: ChatOptions): Promise<AIResponse> {
    const res = await fetch(`${this.baseUrl}/chat/completions`, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${this.apiKey}`
      },
      body: JSON.stringify({
        model,
        messages: messages.map(m => ({ role: m.role, content: m.content })),
        temperature: options?.temperature ?? 0.7,
        max_tokens: options?.maxTokens ?? 4096,
      })
    })

    if (!res.ok) {
      const err = await res.text()
      throw new Error(`MyNewProvider error: ${res.status} - ${err}`)
    }

    const data = await res.json() as any
    return {
      content: data.choices?.[0]?.message?.content || '',
      model,
      provider: 'mynewprovider',
      usage: data.usage
    }
  }
}
```

**الخطوة 2: سجّله في `src/server.ts` (بعد سطر 368):**

```typescript
if (config.mynewproviderKey) {
  aiManager.registerProvider(new MyNewProvider(config.mynewproviderKey))
  log('AI: MyNewProvider provider registered')
}
```

**الخطوة 3: أضف المفتاح في `src/config.ts` (بعد سطر 55):**

```typescript
mynewproviderKey: overrides.mynewproviderKey || process.env.MYNEWPROVIDER_API_KEY || '',
```

**الخطوة 4: عيّن متغير البيئة:**

```bash
export MYNEWPROVIDER_API_KEY="your-key"
```

**الخطوة 5: أعد تشغيل الجهة:**

```bash
npm start
```

##### ملاحظات مهمة

> ⚠️ **لا تضع مفاتيح API في ملف `.env` داخل المشروع!** لأنها قد تُرفع عن طريق الخطأ إلى GitHub.
>
> ⚠️ **لا تشغل الجهة من مجلد المشروع الرئيسي!** يجب أن يكون `cyberguard-agent/` هو المجلد الحالي.
>
> ⚠️ **تأكد من تشغيل both في Terminal منفصل:**
> - Terminal 1: الجهة المحلية (`cd cyberguard-agent && npm start`)
> - Terminal 2: اللعبة (`cd cyber-guardians-mobile && npm run dev`)

##### الاتصال بالوكيل

1. افتح تبويب **🤖 وكيل** في لوحة Tools
2. أدخل رابط الوكيل: `ws://localhost:3002`
3. (اختياري) أدخل Token إذا كان مطلوباً
4. اضغط **🔌 اتصال**

##### فحص الكود

1. اختر أداة الفحص:
   - **Semgrep** — تحليل ثابت للعديد من اللغات
   - **CodeQL** — تحليل كود عميق
   - **Slither** — تحليل عقود Solidity
   - **libFuzzer** — اختبار تشويش موجه بالتغطية
2. اختر لغة الكود
3. الصق الكود في الخانة
4. اضغط **🔍 فحص**
5. ستظهر النتائج مع قائمة الثغرات والتحذيرات

##### تنفيذ أوامر

1. اذهب إلى تبويب **تنفيذ**
2. اكتب الأمر المطلوب
3. اضغط **▶ تنفيذ**
4. ستظهر النتيجة في الأسفل

##### بحث عن قدرات

1. اكتب استعلام البحث
2. اضغط **🔍 بحث**
3. ستظهر قائمة القدرات المتاحة
4. اضغط **تثبيت** بجانب القدرة المطلوبة

##### تبويب المثبتة (Installed) 📦

عرض جميع المهارات المثبتة عالمياً على جهازك.

1. اضغط تبويب **📦 مثبتة**
2. تظهر قائمة بـ 257+ مهارة مثبتة
3. اضغط **🔄 تحديث** لإعادة تحميل القائمة
4. يمكنك البحث في المهارات المثبتة

##### تبويب AI 🤖

محادثة AI عبر المزودات المتاحة.

1. اضغط تبويب **🤖 AI**
2. اختر المزود من القائمة (Gemini, Ollama, Groq, HuggingFace, OpenRouter)
3. اكتب رسالتك في خانة الإدخال
4. اضغط **إرسال** أو اضغط `Enter`
5. سيظهر الرد مباشرة

**أمثلة:**
- "اشرح لي كيف работает SQL injection"
- "اكتب دالة لحساب المضروب بلغة Python"
- "ما هي أفضل ممارسات أمن Node.js؟"

##### تبويب الملفات 📁

إدارة الملفات على جهازك مباشرة.

1. اضغط تبويب **📁 ملفات**
2. اكتب مسار المجلد في خانة المسار
3. اضغط **📂 فتح** لعرض الملفات
4. اضغط على أي ملف لقراءته
5. يمكنك إنشاء ملفات ومجلدات جديدة
6. يمكنك حذف ونقل الملفات

##### تبويب البحث 🔎

بحث في محتوى الملفات باستخدام أنماط regex.

1. اضغط تبويب **🔎 بحث**
2. اكتب نمط البحث (regex) في خانة النمط
3. اكتب مسار البحث في خانة المسار
4. اضغط **🔍 بحث**
5. ستظهر النتائج مع اسم الملف ورقم السطر والمحتوى

**أمثلة على الأنماط:**
- `function\s+\w+` — البحث عن دوال
- `import.*from` — البحث عن imports
- `TODO|FIXME` — البحث عن ملاحظات

##### تبويب OpenCode 💻

تشغيل OpenCode agent لكتابة الكود.

1. اضغط تبويب **💻 OpenCode**
2. اكتب طلبك في خانة الإدخال
3. اضغط **▶ تشغيل** أو اضغط `Enter`
4. سيقوم OpenCode بتنفيذ المهمة (حد أقصى 300 ثانية)
5. يمكنك أيضاً تشغيل تطبيق سطح المكتب عبر زر **🖥️ تشغيل OpenCode Desktop**

**ملاحظات:**
- OpenCode قد يواجه مشاكل في بعض الإصدارات — تطبيق سطح المكتب يعمل بشكل أفضل
- النموذج: `provider/model` (مثل: `google/gemini-2.0-flash`)

---

#### 6. إحصائيات (Analytics) 📊

عرض إحصائيات استخدام المنصة.

##### العرض اليومي

1. اضغط تبويب **📊 إحصائيات**
2. اختر **يومي** من الألسنة
3. اختر التاريخ من التقويم
4. ستظهر: عدد الاستخدامات، أعلى العناصر، توزيع الأنواع

##### العرض الأسبوعي

1. اختر **أسبوعي** من الألسنة
2. ستظهر إحصائيات الأسبوع مع التوزيع اليومي

##### العرض الشهري

1. اختر **شهري** من الألسنة
2. ستظهر إحصائيات الشهر مع معدل النمو

##### أعلى العناصر المستخدمة

- في تبويب **عناصر**، ستظهر قائمة بأكثر العناصر استخداماً
- يمكنك التصفية حسب النوع: قدرات/أدوات/اتصالات/جميع

---

#### 7. نسخ احتياطي (Backup) 💾

إنشاء واستعادة النسخ الاحتياطية.

##### إنشاء نسخة احتياطية

1. اضغط **+ نسخة جديدة**
2. اختر المصدر: **محلي** / **GitHub** / **Google Drive**
3. اضغط **إنشاء**
4. ستظهر النسخة في القائمة مع: التاريخ، الحجم، SHA-256 checksum

##### استعادة نسخة

1. اضغط على النسخة المطلوبة من القائمة
2. اضغط **استعادة**
3. اضغط **تأكيد** في النافذة المنبثقة

##### مزامنة مع GitHub

1. اذهب إلى إعدادات النسخ الاحتياطي
2. أدخل **GitHub Token** و**اسم المستودع** (owner/repo)
3. اضغط **💾 حفظ**
4. اضغط **⬆️ رفع إلى GitHub** للرفع
5. اضغط **⬇️ تحميل من GitHub** للتحميل

##### مزامنة تلقائية

- فعّل **المزامنة التلقائية** في الإعدادات
- يتم رفع نسخة احتياطية كل فترة محددة

##### حذف نسخة

1. اضغط **🗑️** بجانب النسخة
2. اضغط **تأكيد الحذف**

---

#### 8. بحث متقدم (Advanced Search) 🔍

بحث ذكي في 7 متاجر مختلفة.

##### البحث

1. اكتب كلمة البحث في شريط البحث
2. اختر أنواع العناصر المطلوب البحث فيها:
   - 📋 قدرات، 🔌 أدوات، 🔗 اتصالات، 📚 معرفة
   - 📝 تعليمات، 🔄 تغييرات، 📊 استخدام
3. اضغط **🔍 بحث** أو `Enter`
4. ستظهر النتائج مع مدة البحث وعدد النتائج

##### البحث الصوتي

- اضغط زر **🎤** بجانب شريط البحث
- تحدث بالعربية أو الإنجليزية
- سيُحوّل كلامك إلى نص ويبحث تلقائياً

##### حفظ عمليات البحث

1. بعد البحث، اضغط **💾 حفظ البحث**
2. أدخل اسماً للبحث
3. يظهر في تبويب **بحث محفوظة**

##### الاقتراحات

- عند الكتابة، تظهر اقتراحات تلقائية
- اضغط على أي اقتراح للبحث عنه مباشرة

##### السجل

- تبويب **السجل** يعرض عمليات البحث السابقة
- اضغط على أي عملية لإعادة البحث

---

#### 9. مساعد AI (AI Assistant) 🤖

مساعد ذكي لتلخيص النصوص واستخراج المعلومات.

##### تلخيص النصوص

1. اذهب إلى تبويب **📝 تلخيص**
2. الصق أو اكتب النص المطلوب تلخيصه
3. اختر أسلوب التلخيص:
   - **نقاط رئيسية** — ملخص مختصر بالنقاط
   - **فقرات** — ملخص على شكل فقرات
   - **جداول** — تنظيم المعلومات في جداول
4. اضغط **تلخيص**
5. ستظهر النتيجة مع إحصائيات (طول النص الأصلي/المختصر)

##### استخراج الكلمات المفتاحية

- يتم استخراجها تلقائياً عند التلخيص

##### بحث ذكي

1. اذهب إلى تبويب **🔍 بحث ذكي**
2. اكتب سؤالك
3. اضغط **بحث**
4. ستظهر النتائج مع الاقتراحات

##### السجل

- تبويب **📜 السجل** يعرض عمليات التلخيص والبحث السابقة

---

#### 10. تعاون (Collaboration) 🤝

مشاركة وتصدير البيانات.

##### إنشاء رابط مشاركة

1. اذهب إلى تبويب **🔗 مشاركة**
2. اكتب رسالة المشاركة
3. اختر المنصة: WhatsApp، Telegram، Twitter، Facebook، Email، نسخ، QR Code
4. اضغط على أيقونة المنصة

##### التصدير

1. اذهب إلى تبويب **📤 تصدير**
2. اختر صيغة التصدير:
   - **JSON** — بيانات منظمة
   - **Markdown** — نص منسق
   - **HTML** — صفحة ويب
   - **CSV** — جدول بيانات
3. اضغط **تصدير**
4. يُحمّل الملف تلقائياً

##### إدارة المشاركين

1. اذهب إلى تبويب **👥 مشاركون**
2. اضغط **+ إضافة مشارك**
3. أدخل: الاسم، البريد الإلكتروني، الدور (مُشاهد/محرر/مدير)
4. اضغط **حفظ**

##### روابط المشاركة

- تبويب **🔗 روابط** يعرض جميع الروابط المُنشأة
- اضغط **🗑️** لإلغاء أي رابط

---

#### 11. أمان (Security) 🔒

أدوات التشفير والتجزئة والأمان.

##### تشفير البيانات

1. اذهب إلى تبويب **🔐 تشفير**
2. أدخل النص المطلوب تشفيره
3. أدخل كلمة المرور
4. اختر الخوارزمية: **AES-GCM** أو **AES-CBC**
5. اضغط **تشفير**
6. ستظهر النتيجة المشفرة (JSON) مع إمكانية النسخ

##### فك تشفير البيانات

1. اذهب إلى تبويب **🔓 فك تشفير**
2. الصق البيانات المشفرة
3. أدخل كلمة المرور
4. اضغط **فك التشفير**
5. ستظهر النتيجة الأصلية

##### تجزئة كلمات المرور

1. اذهب إلى تبويب **#️⃣ تجزئة**
2. أدخل كلمة المرور أو النص
3. اختر الخوارزمية: **SHA-256** / **SHA-384** / **SHA-512**
4. اضغط **تجزئة**
5. ستظهر القيمة المُجزأة

##### القفل التلقائي

- في إعدادات الأمان، فعّل **القفل التلقائي**
- يُقفل لوحة AI تلقائياً بعد فترة عدم نشاط
- لإلغاء القفل: أدخل كلمة المرور

##### سجل النشاط

- تبويب **📜 النشاط** يعرض جميع العمليات الأمنية
- يمكنك مسح السجل بالكامل

---

#### 12. تقويم (Calendar) 📅

إدارة المهام والمواعيد.

##### إنشاء مهمة

1. اضغط **+ مهمة جديدة**
2. أدخل البيانات:
   - **العنوان**: عنوان المهمة
   - **الوصف**: تفاصيل إضافية
   - **تاريخ الاستحقاق**: التاريخ والوقت
   - **الأولوية**: منخفضة/متوسطة/مرتفعة
   - **التصنيف**: تعلم/مراجعة/ dự án / عام
   - **التذكير**: فعّل/عطّل + مدة التذكير (بالدقائق)
3. اضغط **حفظ**

##### عرض الشهر

- يُعرض تقويم شهري بالكامل
- الأيام التي بها مهام تظهر بعلامة ملونة
- اضغط على أي يوم لعرض مهامه

##### الأولوية والتصنيف

- **منخفضة** 🟢 — مهمة عادية
- **متوسطة** 🟡 — مهمة مهمة
- **مرتفعة** 🔴 — مهمة عاجلة

##### تعديل/حذف مهمة

- اضغط على المهمة في القائمة
- اضغط **تعديل** أو **حذف**

---

#### 13. تقارير (Reports) 📋

إنشاء تقارير مخصصة بأنواع مختلفة.

##### إنشاء تقرير

1. اضغط **+ تقرير جديد**
2. أدخل البيانات:
   - **الاسم**: اسم التقرير
   - **النوع**: استخدام/أداء/أمان/activities/مخصص
   - **الفترة**: يوم/أسبوع/شهر/مخصص
   - **الصيغة**: جدول/رسم بياني/ملخص
   - **المقياسات**: اختر ما تريد تضمينه
   - **تاريخ البداية والنهاية**: للنوع المخصص
3. اضغط **إنشاء**

##### عرض التقرير

- اضغط **▶ إنشاء** بجانب التقرير
- يظهر في نافذة منفصلة مع البيانات والرسوم البيانية

##### تصدير التقرير

- اضغط **📤 تصدير** لتحميل التقرير بصيغة JSON أو CSV

---

### تبويب 📁 مشروع (4 تبويبات فرعية)

#### 1. معرفة 📚

رفع وإدارة ملفات المعرفة.

##### رفع ملفات

1. اضغط **+ رفع ملف** أو اسحب الملفات وأفلتها
2. الحد الأقصى: 10 ميجابايت للملف الواحد
3. الأنواع المدعومة: نصوص، صور، فيديو، صوت، JSON
4. يظهر حجم الملف ونوعه

##### البحث في المعرفة

1. اكتب كلمة البحث في شريط البحث
2. ستظهر الملفات المطابقة
3. اضغط على ملف لعرض محتواه

##### حذف ملف

1. اضغط **🗑️** بجانب الملف
2. يُحذف فوراً

---

#### 2. تعليمات 📝

تخصيص سلوك AI.

##### تخصيص دور AI

- اكتب تعليمات مخصصة لدور AI في هذا المشروع
- مثال: "أنت مساعد متخصص في أمن الشبكات فقط"

##### نبرة الرد

اختر نبرة الرد من القائمة:
- 💼 مهني
- 😊 ودي
- 🎩 رسمي
- 👋 عفوي
- 🎓 أكاديمي
- 🎨 إبداعي

##### قالب مخصص

- يمكنك كتابة قالب رد مخصص
- يُستخدم كإرشاد لـ AI في جميع الردود

---

#### 3. محادثات 💬

إدارة المحادثات المشتركة.

##### مشاركة المحادثات

1. اختر المحادثة المطلوبة
2. اضغط **🔗 مشاركة**
3. انسخ الرابط أو أرسله مباشرة

##### تثبيت محادثة

- اضغط **📌 تثبيت** لتثبيت محادثة في أعلى القائمة
- يظهر دائمًا بغض النظر عن تاريخها

---

#### 4. تاريخ 🕐

استعادة لقطات سابقة.

##### استعادة لقطة

1. اعرض قائمة اللقطات
2. اضغط على اللقطة المطلوبة
3. اضغط **استعادة**
4. سيُستعاد المحتوى كما كان في ذلك الوقت

##### إنشاء لقطة يدوية

- اضغط **📸 إنشاء لقطة** لإنشاء نقطة استعادة يدوية

##### مقارنة اللقطات

- اختر لقطتين للمقارنة
- ستظهر الفروقات بينهما

---

### تبويب ⚙ إعدادات

تغيير مزود AI والنماذج وإعدادات الاتصال.

#### تغيير المزود والنموذج

1. اختر **المزود** من القائمة:
   - Google Gemini (مجاني)
   - OpenAI
   - Anthropic
   - أو مزود مخصص
2. اختر **النموذج** من القائمة المنسدلة
3. أدخل **API Key** إذا كان مطلوباً

#### Worker URL

لإعداد Worker Proxy الآمن:

1. اضغط **📖 عرض دليل الاستخدام** لعرض الخطوات التفصيلية
2. أدخل رابط Worker (مثال: `https://my-proxy.workers.dev`)
3. أدخل Auth Token (اختياري)
4. اضغط **💾 حفظ**
5. اضغط **🔌 اختبار** للتأكد من عمله

**لماذا Worker Proxy؟**
- يمنع XSS من سرقة API keys
- يُمرّر الطلبات عبر Cloudflare بدلاً من المتصفح مباشرة

#### Direct API Mode

- فعّل **وضع الاتصال المباشر** للاتصال بالـ API مباشرة بدون Worker
- مفيد للاختبار المحلي
- ⚠️ Less secure than Worker Proxy

---

### تبويب 🎨 إعدادات UI

تخصيص مظهر اللعبة.

#### 5 سمات (Themes)

اختر من 5 سمات جاهزة:
- 🔵 Cyber Blue — أزرق سايبر
- 🟣 Neon Purple — بنفسجي نيون
- 🟢 Matrix Green — أخضر الماتريكس
- 🔴 Dark Red — أحمر داكن
- 🟡 Gold — ذهبي

#### اللغة

اختر لغة الواجهة:
- 🇸🇦 العربية
- 🇬🇧 English

#### حجم الخط

- **حجم النص الأساسي**: 12–28px (الافتراضي: 16px)
- **حجم العناوين**: 14–40px (الافتراضي: 24px)
- **حجم الكود**: 10–24px (الافتراضي: 14px)
- **حجم النص الثانوي**: 10–20px (الافتراضي: 13px)

#### ألوان النص

- لون النص الأساسي
- لون العناوين
- لون التمييز
- لون النص الثانوي

#### معاينة حية

- جميع التغييرات تظهر فوراً في المعاينة

---

## 3. الجهة المحلية (Local Agent)

### نظرة عامة

Local Agent هو خادم محلي يعمل على جهازك ويوصل المتصفح بالأدوات الخارجية مثل Semgrep و CodeQL و Slither و libFuzzer. كما يوفر نظام أداة متعدد (Aider + Cline + Agent مخصص) ومقدمات AI متعددة وعمليات ملفات وبحث محتوى. يمكن لطالبي وهيئة تدريس استخدام الوكيل.

### المتطلبات

| النظام | الأدوات المطلوبة |
|--------|-----------------|
| **Windows** | Node.js 18+، npm |
| **macOS** | Node.js 18+، brew (اختياري) |
| **Linux** | Node.js 18+، apt/yum/pacman |

### التثبيت

```bash
# التثبيت العالمي
npm install -g @cyberguard/agent

# التحقق من التثبيت
cyberguard-agent --version
```

### التشغيل عبر Docker (مُوصى به)

```bash
# تشغيل كل شيء (Agent + Frontend)
docker-compose up -d

# عرض السجلات
docker-compose logs -f

# إيقاف كل شيء
docker-compose down

# تشغيل Agent فقط
cd cyberguard-agent
docker build -t cyberguard-agent .
docker run -d -p 3001:3001 cyberguard-agent
```

### التشغيل عبر npm

```bash
# التشغيل الأساسي
cyberguard-agent start --port 3002

# التشغيل مع ملف إعدادات كامل
cyberguard-agent start --port 3002 --profile full

# التشغيل مع مصادقة
cyberguard-agent start --port 3002 --token my-secret-token
```

### إعداد مفاتيح AI

```bash
# إعداد المفاتيح (اختياري)
export GEMINI_API_KEY="your-gemini-key"
export GROQ_API_KEY="your-groq-key"
export HUGGINGFACE_API_KEY="your-hf-key"
export OPENROUTER_API_KEY="your-openrouter-key"
```

### الاستخدام

1. **الاتصال**: من تبويب 🤖 وكيل في لوحة Tools
2. **فحص الكود**: اختر الأداة، الصق الكود، اضغط فحص
3. **تنفيذ أوامر**: اكتب الأمر واضغط تنفيذ
4. **البحث عن قدرات**: اكتب استعلام واضغط بحث
5. **محادثة AI**: اختر المزود وابدأ المحادثة
6. **إدارة الملفات**: تصفح وأدر الملفات على جهازك
7. **بحث المحتوى**: ابحث في محتوى الملفات بـ regex
8. **OpenCode**: شغّل OpenCode agent لكتابة الكود

### الأدوات المدعومة

| الأداة | الوظيفة | المدخلات | المخرجات |
|--------|---------|----------|----------|
| **Semgrep** | تحليل ثابت | كود + لغة | نتائج SARIF |
| **CodeQL** | تحليل عميق | كود + لغة | نتائج SARIF |
| **Slither** | تحليل Solidity | كود عقد ذكي | تقرير ثغرات |
| **libFuzzer** | اختبار تشويش | كود C/C++ | اصطدادات + تغطية |

### مقدمات AI المدعومة

| المزود | الميزة | المفتاح | الحد المجاني |
|--------|--------|---------|--------------|
| **Gemini** | المزود الافتراضي | `GEMINI_API_KEY` | 1500 طلب/يوم |
| **Ollama** | يعمل محلياً | لا يحتاج مفتاح | غير محدود |
| **Groq** | سرعة عالية | `GROQ_API_KEY` | 30 طلب/دقيقة |
| **HuggingFace** | موديلات متنوعة | `HUGGINGFACE_API_KEY` | 1000 طلب/يوم |
| **OpenRouter** | وصول لموديلات متعددة | `OPENROUTER_API_KEY` | حسب الموديل |

### عمليات الملفات المدعومة

| العملية | الوصف |
|---------|-------|
| `read` | قراءة ملف |
| `write` | كتابة/إنشاء ملف |
| `list` | سرد محتويات مجلد |
| `delete` | حذف ملف |
| `move` | نقل/إعادة تسمية ملف |
| `mkdir` | إنشاء مجلد |
| `exists` | فحص وجود ملف |
| `search` | بحث في أسماء الملفات |

### اللغات المدعومة

JavaScript، TypeScript، Python، Java، Go، Ruby، PHP، C، C++، Rust، Solidity

---

## 4. نصائح وأفضل الممارسات

### للطلاب

1. **ابدأ بالأساسيات**: اكتمل المستويات بالترتيب
2. **استخدم التلميحات**: لا تتردد في استخدام 💡 عند العثور
3. **اكتب أسئلة محددة**: كلما كان السؤال أوضح، كان الرد أفضل
4. **جرّب Deepthink**: للمشاكل المعقدة، فعّل 🧠 للتفكير العميق
5. **استخدم البحث**: فعّل 🔍 للحصول على أحدث المعلومات
6. **تابع تقدمك**: راقب نقاطك وشاراتك من القائمة الرئيسية
7. **أكمل المهام اليومية**: للمكافآت الإضافية

### للمعلمين

1. **استخدم هيئة التدريس**: لتعديل المحتوى عبر المحادثة
2. **أنشئ نسخاً احتياطية**: بشكل دوري عبر GitHub أو Google Drive
3. **خصص المعايير**: عدّل أسئلة الاختبار والصعوبة حسب فصلك
4. **تتبع تقدم الطلاب**: من لوحة التقارير
5. **أضف محتوى جديداً**: أنشئ مستويات وتحديات مخصصة
6. **استخدم التقويم**: لجدولة مواعيد المراجعة والاختبارات
7. **شارك النتائج**: صدّر تقارير CSV لإدارة المدرسة

### للأمان

1. **لا تشارك API Keys**: حافظ على مفاتيحك سرية
2. **استخدم Worker Proxy**: للحماية من XSS
3. **فعّل القفل التلقائي**: عند مغادرة الجهاز
4. **استخدم التشفير**: للبيانات الحساسة
5. **راجع سجل النشاط**: بشكل دوري
6. ** backed up بانتظام**: انسخ احتياطي قبل التعديلات الكبيرة

### لإدارة الأداء

1. **مسح التخزين المؤقت**: إذا كانت اللعبة بطيئة
2. **قلل النماذج الثقيلة**: استخدم Gemini Flash بدل النماذج الكبيرة
3. **عطّل البحث**: إذا لم يكن مطلوباً
4. **استخدم Direct API Mode**: للسرعة القصوى محلياً

---

## مشاكل معروفة (Known Bugs)

> آخر تدقيق: 2026-06-23 — التقرير الكامل في `AUDIT_REPORT.md`

### الوكيل المحلي (Local Agent)

|المشكلة | التأثير | الحل المقترح |
|--------|---------|-------------|
| **HuggingFace استجابة خاطئة** | مزود HuggingFace غير وظيفي | إصلاح تحليل الاستجابة في `providers.ts` |
| **حالة AI Fallback** | الطلبات المتزامنة تتداخل | استخدام متغير محلي بدلاً من حالة عامة |
| **grep include غير متصل** | فلتر include وهمي | توصيل `include` في `fileOp` handler |
| **OpenCode أزرار المزود** | أزرار المزود وهمية | إضافة حالة `opencodeProvider` منفصلة |

### للعلم

- **OpenCode CLI `run`** — يواجه مشاكل في الإصدار v1.15.10 — تطبيق سطح المكتب يعمل بشكل أفضل
- **مهلة OpenCode** — 300 ثانية (5 دقائق) — يُنصح باستخدام OpenCode Desktop للمهام الطويلة
- **HuggingFace** — غير وظيفي حالياً — استخدم Gemini أو Ollama كبديل

---

> **آخر تحديث:** الإصدار 10.1.0 (تدقيق مكتمل)
> **المطور:** YoussefAhamedKamal
> **الاستضافة:** Cloudflare Pages
> **المستودع:** github.com/YoussefAhamedKamal/cyber-guardians-mobile
