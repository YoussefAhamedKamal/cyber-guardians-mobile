# دليل الإعدادات المتقدمة للذكاء الاصطناعي

> دليل عملي شامل لكل مكون في لوحة الإعدادات المتقدمة
> الإصدار: **11.0.0** — نظام الأدوات المتعدد (Aider + Cline + Agent مخصص) + Gemini المجاني + Docker

---

## جدول المحتويات

1. [إعدادات المزود](#provider-settings)
2. [طرق الاتصال](#connection-methods)
3. [إعدادات Gemini — المزود الافتراضي](#gemini-settings)
4. [لوحة AI Panel](#ai-panel)
5. [المهارات (Skills)](#skills)
6. [الأدوات (Plugins)](#plugins)
7. [الاتصالات (Connectors)](#connectors)
8. [Deepthink — التفكير العميق](#deepthink)
9. [Marketplace — سوق القوالب](#marketplace)
10. [البحث المتقدم](#advanced-search)
11. [النسخ الاحتياطي](#backup)
12. [الإعدادات المتقدمة](#advanced-features)
13. [نظام الأدوات المتعدد](#multi-tool-system)
14. [مقدمات AI للوكيل المحلي](#agent-ai-providers)

---

## 1. إعدادات المزود {#provider-settings}

### نظرة عامة

اختر مزود الذكاء الاصطناعي المناسب لاحتياجاتك. كل مزود له مزايا وقيود مختلفة.

### OpenAI

| النموذج | السرعة | الجودة | التكلفة |
|---------|--------|--------|---------|
| **GPT-4o Mini** | سريع | متوسطة | رخيص |
| **GPT-4o** | متوسط | عالية | متوسط |
| **GPT-3.5 Turbo** | سريع جداً | جيدة | رخيص جداً |

**الإعداد:**
1. اذهب إلى `AI Panel → ⚙️ إعدادات المزود`
2. اختر "OpenAI"
3. أدخل API Key من `platform.openai.com`
4. اختر النموذج المطلوب
5. اضغط "💾 حفظ"

### OpenRouter

مزود بوابة لعدة نماذج مفتوحة المصدر:

| النموذج | المطور | المزايا |
|---------|--------|---------|
| **Llama 3.2** | Meta | مفتوح المصدر، أداء ممتاز |
| **Mistral** | Mistral AI | أوروبي، خصوصية عالية |
| **Gemini** | Google | مدعوم عبر OpenRouter |

**ملاحظة:** OpenRouter المجاني يدعم 50 طلب/يوم فقط. للحصول على المزيد، شحن حسابك بـ $10.

### Google Gemini — المزود الافتراضي ⭐

| النموذج | السرعة | السياق | التكلفة |
|---------|--------|--------|---------|
| **Gemini 3.5 Flash** | أسرع | 1M توكن | مجاني |
| **Gemini 3.1 Flash Lite** | أسرع جداً | 1M توكن | مجاني |
| **Gemini 3 Flash** | سريع | 1M توكن | مجاني |

**لماذا Gemini هو الأفضل؟**
- **مجاني بالكامل:** حتى 1500 طلب/يوم
- **سياق ضخم:** حتى 1,000,000 توكن
- **سرعة عالية:** مثالي للمحادثات الطويلة
- **دعم عربي ممتاز**

### Ollama (Localhost)

تشغيل النماذج محلياً على جهازك — بدون إنترنت:

1. ثبّت Ollama من `ollama.ai`
2. شغّل `ollama serve` في الطرفية
3. اختر "Ollama" كمزود في الإعدادات
4. اختر النموذج المحمل (llama3, mistral, etc.)

**المزايا:** خصوصية تامة، بدون إنترنت، بدون تكلفة
**العيوب:** يحتاج جهاز قوي (GPU)، أبطأ من السحابة

### Custom (OpenAI-compatible)

أي API يتوافق مع صيغة OpenAI:

```
الرابط: https://your-api.com/v1/chat/completions
API Key: sk-xxxxx
النموذج: model-name
```

**مثالي لـ:**
- نماذج مخصصة في مؤسستك
- خوادم بديلة (vLLM, LocalAI)
- اختبار نماذج محلية

---

## 2. طرق الاتصال {#connection-methods}

### Worker Proxy (Cloudflare Workers) — مُوصى به

الموصل عبر Cloudflare Workers作為ال intermediary:

**كيف يعمل:**
```
المتصفح → Cloudflare Worker → مزود AI
```

**المزايا:**
- إخفاء مفاتيح API عن المستخدم
- حماية من CORS
- rate limiting آلي
- سجل استخدام

**الإعداد:**
1. اذهب إلى `AI Panel → ⚙️ إعدادات الاتصال`
2. اختر "Worker Proxy"
3. أدخل رابط Worker الخاص بك
4. اضغط "💾 حفظ"

### Direct API — تحذير أمني ⚠️

الاتصال المباشر من المتصفح إلى مزود AI:

**الاستخدام:**
- للتطوير المحلي فقط
- بدون نشر على الإنترنت

**تحذيرات أمنية:**
- **⚠️** مفاتيح API تظهر في كود JavaScript
- **⚠️** أي شخص يمكنه رؤية المفاتيح من DevTools
- **⚠️** لا تستخدم Direct API في بيئة الإنتاج
- **⚠️** المفاتيح المكشوفة قد تُسخَّر من قبل آخرين

**الإعداد (للمطورين فقط):**
1. اختر "Direct API"
2. أدخل Endpoint URL و API Key
3. اضغط "💾 حفظ"
4. **⚠️ تذكر:** لا تنشر هذا الإعداد على الإنترنت

---

## 3. إعدادات Gemini — المزود الافتراضي {#gemini-settings}

### الحصول على API Key من Google AI Studio

#### الخطوة 1: اذهب إلى Google AI Studio
```
1. افتح المتصفح
2. اذهب إلى aistudio.google.com
3. سجل الدخول بحساب Google
```

#### الخطوة 2: أنشئ API Key
```
1. اضغط على "Get API key" في الشريط الجانبي
2. اضغط "Create API key"
3. اختر المشروع أو أنشئ مشروع جديد
4. انسخ API Key (يبدأ بـ AIza...)
5. اضغط "✅ Done"
```

#### الخطوة 3: أدخل المفتاح في اللعبة
```
1. افتح AI Panel
2. اذهب إلى ⚙️ إعدادات المزود
3. اختر "Google Gemini"
4. الصق API Key في الحقل المناسب
5. اضغط "💾 حفظ"
6. اضغط "🔍 اختبار الاتصال" للتأكد
```

### Rate Limits — حدود الاستخدام

| الباقة | الطلبات/يوم | الطلبات/دقيقة | التكلفة |
|--------|-------------|---------------|---------|
| **Free Tier** | 1,500 | 15 | مجاني |
| **Pay-as-you-go** | غير محدود | 600 | $0.075/1K توكن |
| **Cloud** | غير محدود | 600+ | حسب الاستخدام |

**ملاحظات:**
- Free Tier كافي للاستخدام اليومي العادي
- إذا وصلت الحد، انتظر 24 ساعة أو ادفع حسب الاستخدام
- حد الطلبات/دقيقة أهم من الطلبات/يوم للاستخدام العادي

### النماذج المتاحة

| النموذج | الحالة | السياق | الأفضل لـ |
|---------|--------|--------|----------|
| **gemini-3.5-flash** | نشط ✅ | 1M توكن | الاستخدام اليومي — الأسرع |
| **gemini-3.1-flash-lite** | نشط ✅ | 1M توكن | الأجهزة الضعيفة — أخف وزناً |
| **gemini-3-flash** | نشط ✅ | 1M توكن | التوازن بين السرعة والجودة |
| **gemini-2.0-flash** | مُغلق ❌ | — | لا تستخدم — أُغلق يونيو 2026 |
| **gemini-1.5-pro** | مُغلق ❌ | — | لا تستخدم — أُغلق يونيو 2026 |

### النموذج الافتراضي: Gemini 3.5 Flash

**لماذا هو الأفضل؟**
- أسرع نموذج في عائلة Gemini
- جودة ممتازة للمحادثات والتوليد
- سياق 1M توكن (يمكنك إرسال مجلد كامل)
- مجاني بالكامل مع Free Tier

---

## 4. لوحة AI Panel {#ai-panel}

### نظرة عامة

لوحة التحكم المركزية لإدارة كل ما يخص الذكاء الاصطناعي في المشروع.

### 6 تبويبات رئيسية

```
┌─────────────────────────────────────┐
│  🤖 AI Panel                        │
│  ├── 💬 محادثة AI                   │  ← المحادثة مع AI
│  ├── 🛠️ أدوات                       │  ← المهارات والأدوات
│  ├── 📁 مشروع                       │  ← إدارة المشروع
│  ├── ⚙️ إعدادات                     │  ← الإعدادات العامة
│  ├── 🔍 بحث                         │  ← البحث المتقدم
│  └── 📊 إحصائيات                    │  ← التقارير
└─────────────────────────────────────┘
```

### تبويبات فرعية في أدوات (13 تبويباً)

```
🛠️ أدوات
├── 🎯 Skills (المهارات)
├── 🔌 Plugins (الأدوات)
├── 🔗 Connectors (الاتصالات)
├── 🧠 Deepthink (التفكير العميق)
├── 🌐 Web Search (بحث الويب)
├── 📝 Code Analysis (تحليل الكود)
├── 🔒 Security Scan (فحص الأمان)
├── 📊 Data Processing (معالجة البيانات)
├── 🎨 Content Generation (توليد المحتوى)
├── 🗣️ Translation (الترجمة)
├── 📚 Documentation (التوثيق)
├── 🧪 Testing (الاختبارات)
└── 🔧 Utilities (الأدوات المساعدة)
```

### تبويبات مشروع (4 تبويبات)

```
📁 مشروع
├── 📄 الملفات (Files)
├── 💾 النسخ الاحتياطي (Backup)
├── 📋 سجل التغييرات (Changelog)
└── 📊 حالة المشروع (Status)
```

---

## 5. المهارات (Skills) {#skills}

### 12 قالب جاهز

| # | المهارة | الوظيفة | مثال |
|---|---------|---------|------|
| 1 | **تحليل الكود** | تحليل ملفات المصدر | "حلل ملف auth.ts" |
| 2 | **توليد الوثائق** | إنشاء توثيق تلقائي | "أنشئ API docs" |
| 3 | **فحص الثغرات** | البحث عن ثغرات أمنية | "افحص المشروع" |
| 4 | **اختبارات تلقائية** | كتابة اختبارات | "اكتب اختبارات لـ login" |
| 5 | **ترجمة النصوص** | ترجمة محتوى | "ترجم إلى الإنجليزية" |
| 6 | **تحسين الأداء** | تحليل وتحسين | "حسّن أداء هذا الملف" |
| 7 | **تنسيق الكود** | تنظيف وتنسيق | "نسّق الكود" |
| 8 | **إعادة هيكلة** | تحسين البنية | "أعد هيكلة الدالة" |
| 9 | **شرح الكود** | شرح مفصل | "اشرح كيف يعمل هذا" |
| 10 | **اكتشاف الأخطاء** | البحث عن bugs | "جد الأخطاء" |
| 11 | **تحسين الأمان** | تعزيز الحماية | "حسّن الأمان" |
| 12 | **إنشاء ملخص** | ملخص سريع | "لخّص هذا الملف" |

### إنشاء مهارة مخصصة

#### الخطوة 1: افتح واجهة المهارات
```
AI Panel → 🛠️ أدوات → 🎯 Skills → + إنشاء مهارة جديدة
```

#### الخطوة 2: حدد خصائص المهارة

| الحقل | الوصف | مثال |
|-------|-------|------|
| **الاسم** | اسم المهارة | "فحص API" |
| **الوصف** | وظيفة المهارة | "فحص أمان واجهات API" |
| **النوع** | analysis / generation / scanning | "scanning" |
| **القالب** | JSON template | `{ "endpoint": "...", "method": "GET" }` |
| **الشروط** | شروط التفعيل | `"type": "api"` |

#### الخطوة 3: حدد الـ System Prompt

```
أنت خبير أمني متخصص في فحص API.
عندما يتلقى المستخدم طلباً:
1. حدد نوع الـ API
2. افحص كل endpoint
3. اكتشف الثغرات المحتملة
4. اقترح الحلول
```

#### الخطوة 4: احفظ وفعّل
```
1. اضغط "💾 حفظ المهارة"
2. فعّلها من قائمة المهارات
3. جرّبها في محادثة AI
```

### شروط التفعيل

| الشرط | الوصف |
|-------|-------|
| **مطابقة الكلمات** | يظهر عندما يتضمن الطلب كلمات معينة |
| **نوع الملف** | يظهر فقط مع أنواع ملفات محددة |
| **السياق** | يظهر بناءً على سياق المحادثة |
| **المشروع** | يظهر فقط في مشاريع محددة |
| **manual** | لا يظهر تلقائياً — يجب استدعاؤه يدوياً |

---

## 6. الأدوات (Plugins) {#plugins}

### 10 أدوات جاهزة

| # | الأداة | الوظيفة | Endpoint |
|---|--------|---------|----------|
| 1 | **HTTP Request** | إرسال طلبات HTTP | حسب الإعداد |
| 2 | **File Read** | قراءة ملفات المشروع | محلي |
| 3 | **File Write** | كتابة ملفات | محلي |
| 4 | **Database Query** | استعلام قاعدة بيانات | حسب الإعداد |
| 5 | **Email Send** | إرسال بريد إلكتروني | SMTP |
| 6 | **Slack Notify** | إرسال إشعار Slack | Webhook |
| 7 | **GitHub API** | التفاعل مع GitHub | REST API |
| 8 | **Cloud Storage** | رفع/تحميل ملفات | S3/GCS |
| 9 | **AI Inference** | استدعاء نموذج آخر | API |
| 10 | **Custom Script** | تنفيذ سكربت مخصص | محلي |

### تنفيذ مع Endpoint و Parameters

#### إعداد أداة جديدة

```
AI Panel → 🛠️ أدوات → 🔌 Plugins → + إضافة أداة
```

#### الحقول المطلوبة

| الحقل | الوصف | مثال |
|-------|-------|------|
| **الاسم** | اسم الأداة | "إشعار Discord" |
| **النوع** | GET / POST / PUT / DELETE | POST |
| **Endpoint** | رابط الـ API | `https://discord.com/api/webhooks/...` |
| **Content-Type** | نوع المحتوى | `application/json` |
| **Headers** | رؤوس الطلب | `Authorization: Bearer xxx` |
| **Parameters** | معاملات الطلب | `{ "content": "..." }` |

#### مثال: إرسال إشعار Slack

```json
{
  "name": "Slack Alert",
  "method": "POST",
  "endpoint": "https://hooks.slack.com/services/xxx",
  "contentType": "application/json",
  "headers": {},
  "parameters": {
    "text": "🚨 تنبيه أمني: {{alert_message}}",
    "channel": "#security"
  }
}
```

### أنواع المصادقة

| النوع | الوصف | مثال |
|-------|-------|------|
| **None** | بدون مصادقة | APIs مفتوحة |
| **API Key** | مفتاح API في Header | `X-API-Key: xxx` |
| **Bearer Token** | توكن في Authorization | `Authorization: Bearer xxx` |
| **Basic Auth** | اسم مستخدم وكلمة مرور | `Authorization: Basic base64` |
| **OAuth 2.0** | مصادقة OAuth | Client ID + Secret |
| **Custom Headers** | رؤوس مخصصة | أي تكوين |

---

## 7. الاتصالات (Connectors) {#connectors}

### 13 مزود

| # | المزود | النوع | المزايا |
|---|--------|-------|---------|
| 1 | **OpenAI** | API | GPT-4o, GPT-3.5 |
| 2 | **Google Gemini** | API | مجاني، سياق ضخم |
| 3 | **OpenRouter** | Gateway | عدة نماذج |
| 4 | **Ollama** | Local | محلي، خصوصية |
| 5 | **Anthropic** | API | Claude |
| 6 | **Mistral** | API | أوروبي |
| 7 | **Cohere** | API | نماذج متعددة |
| 8 | **Groq** | API | أسرع برمجياً |
| 9 | **Together AI** | API | مفتوح المصدر |
| 10 | **Replicate** | API | نماذج متنوعة |
| 11 | **Deepseek** | API | تحليل الكود |
| 12 | **xAI** | API | Grok |
| 13 | **Custom** | API | أي OpenAI-compatible |

### مصادقة خاصة لكل مزود

```
OpenAI:
  - API Key من platform.openai.com
  - يبدأ بـ sk-

Gemini:
  - API Key من aistudio.google.com
  - يبدأ بـ AIza

OpenRouter:
  - API Key من openrouter.ai
  - يبدأ بـ sk-or-

Ollama:
  - لا يحتاج API Key
  - فقط URL الخادم (localhost:11434)
```

### اختبار الاتصال

```
1. اختر المزود من القائمة
2. أدخل بيانات الاعتماد
3. اضغط "🔍 اختبار الاتصال"
4. إذا ظهر ✅ — الاتصال يعمل ✅
5. إذا ظهر ❌ — تحقق من البيانات ❌
```

---

## 8. Deepthink — التفكير العميق {#deepthink}

### 3 خطوات تفكير

Deepthink يحلل الطلبات المعقدة في ثلاث مراحل:

#### الخطوة 1: التحليل الأولي
```
- فهم طلب المستخدم
- تحديد النطاق والأهداف
- جمع المعلومات ذات الصلة
```

#### الخطوة 2: التفكير التفصيلي
```
- تحليل كل جانب بشكل منفصل
- ربط العلاقات بين العناصر
- تحديد المخاطر والفرص
- اقتراح حلول بديلة
```

#### الخطوة 3: الإخراج النهائي
```
- تلخيص النتائج
- تقديم توصيات عملية
- تقديم خطة تنفيذ واضحة
```

### مثال على التفكير العميق

```
المستخدم: "حلل ثغرات نظام المصادقة في المشروع"

Deepthink:

📌 الخطوة 1: التحليل الأولي
- المشروع يستخدم JWT للمصادقة
- يوجد 15 endpoint يتطلب مصادقة
- يتم استخدام bcrypt لكلمات المرور

📌 الخطوة 2: التفكير التفصيلي
- jwt.verify() لا يتحقق من الصلاحية ❌
- لا يوجد rate limiting على login ❌
- كلمة المرور المخزنة في localStorage ⚠️
- لا يوجد logout server-side ⚠️

📌 الخطوة 3: الإخراج النهائي
- 🔴 ثغرة حرجة: كلمة المرور في localStorage
- 🟡 ثغرة متوسطة: عدم التحقق من الصلاحية
- 🟢 تحسين مقترح: إضافة rate limiting
```

### Streaming Support — البث المباشر

يمكنك مشاهدة تفكير AI في الوقت الفعلي:

```
1. اضغط "🧠 Deepthink" في شريط المحادثة
2. اكتب طلبك المعقد
3. شاهد التفكير يظهر سطر بسطر
4. النتيجة النهائية تظهر بعد اكتمال التحليل
```

### System Prompt مخصص

يمكنك تخصيص تعليمات التفكير العميق:

```
AI Panel → 🧠 Deepthink → ⚙️ System Prompt

مثال:
"أنت خبير أمني. حلل كل طلب من زاوية أمنية.
 حدد الثغرات والمخاطر واقترح حلولاً عملية.
 استخدم تصنيف: 🔴 حرج | 🟡 متوسط | 🟢 منخفض"
```

---

## 9. Marketplace — سوق القوالب {#marketplace}

### 35 قالب (12+10+13)

#### قوالب المهارات (12)

| # | القالب | الوصف |
|---|--------|-------|
| 1 | Security Audit | فحص أمني شامل |
| 2 | Code Review | مراجعة الكود |
| 3 | API Testing | اختبار APIs |
| 4 | Database Design | تصميم قواعد البيانات |
| 5 | Documentation | توليد وثائق |
| 6 | Performance | تحسين الأداء |
| 7 | Migration | ترحيل البيانات |
| 8 | Monitoring | مراقبة الأنظمة |
| 9 | CI/CD Pipeline | خطوط النشر |
| 10 | Container | Docker/K8s |
| 11 | GraphQL | تصميم GraphQL |
| 12 | Mobile App | تطبيقات الجوال |

#### قوالب الأدوات (10)

| # | القالب | الوصف |
|---|--------|-------|
| 1 | Webhook Handler | معالجة Webhooks |
| 2 | Email Service | خدمة البريد |
| 3 | File Processor | معالجة الملفات |
| 4 | Data Transform | تحويل البيانات |
| 5 | Cache Manager | إدارة الكاش |
| 6 | Queue System | نظام الطوابير |
| 7 | Search Engine | محرك بحث |
| 8 | Analytics | تحليل البيانات |
| 9 | Notification | نظام الإشعارات |
| 10 | Scheduler | جدولة المهام |

#### قوالب الاتصالات (13)

| # | القالب | الوصف |
|---|--------|-------|
| 1 | REST API | واجهة REST |
| 2 | GraphQL | واجهة GraphQL |
| 3 | WebSocket | اتصال مباشر |
| 4 | gRPC | اتصال gRPC |
| 5 | MQTT | إنترنت الأشياء |
| 6 | Slack Bot | روبوت Slack |
| 7 | Discord Bot | روبوت Discord |
| 8 | Telegram Bot | روبوت Telegram |
| 9 | WhatsApp | واتساب Business |
| 10 | Email SMTP | بريد إلكتروني |
| 11 | SMS Gateway | رسائل نصية |
| 12 | Cloud Storage | تخزين سحابي |
| 13 | Payment Gateway | بوابة الدفع |

### البحث والفلترة

```
1. اذهب إلى 🛠️ أدوات → 🏪 Marketplace
2. استخدم شريط البحث للبحث بالاسم
3. استخدم الفلاتر:
   - النوع: مهارة / أداة / اتصال
   - الحالة: مجاني / مدفوع
   - التقييم: ⭐⭐⭐⭐⭐
4. اضغط "تثبيت" لتحميل القالب
```

---

## 10. البحث المتقدم {#advanced-search}

### 7 متاجر للبحث

| # | المتجر | المحتوى |
|---|--------|---------|
| 1 | **المهارات** | جميع المهارات المثبتة |
| 2 | **الأدوات** | جميع الأدوات المتاحة |
| 3 | **الاتصالات** | جميع الاتصالات المكونة |
| 4 | **المشروع** | ملفات وبيانات المشروع |
| 5 | **سجل التغييرات** | جميع التعديلات السابقة |
| 6 | **المحادثات** | سجل محادثات AI |
| 7 | **القوالب** | قوالب Marketplace |

### Fuzzy Match — البحث الضبابي

لا حاجة لكتابة الكلمة بالضبط:

```
بحث: "auth"
→ يجد: authentication, authorize, auth.ts, AuthProvider

بحث: "سلاك"
→ يجد: Slack, slack-bot, إشعار Slack
```

**كيف يعمل:**
- يبحث في الأسماء والأوصاف
- يدعم الأخطاء الإملائية
- ي优先 النتائج الأكثر تطابقاً

### حفظ عمليات البحث

```
1. نفّذ بحثاً
2. اضغط "💾 حفظ هذا البحث"
3. أدخل اسماً (مثلاً: "أدوات الأمان")
4. البحث يظهر في القائمة المحفوظة
5. يمكنك إعادة تشغيله في أي وقت
```

---

## 11. النسخ الاحتياطي {#backup}

### GitHub Sync — المزامنة مع GitHub

#### الإعداد الأولي

```
1. اذهب إلى ⚙️ إعدادات → النسخ الاحتياطي
2. اختر "GitHub Sync"
3. أدخل:
   - GitHub Token: ghp_xxxxx
   - Owner: username
   - Repo: repo-name
   - Branch: main
4. اضغط "💾 حفظ"
5. اضغط "🔍 اختبار الاتصال"
```

#### أنواع النسخ

| النوع | المحتوى | الصيغة |
|-------|---------|--------|
| **كامل** | كل شيء | JSON |
| **مجزأ** | ملفات محددة | JSON/TS |
| **فوري** | فوري عند التعديل | — |

### SHA-256 Checksum

كل نسخة احتياطية لها بصمة فريدة:

```
النسخة #1: a3f2b8c1d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0c1d2e3f4a5b6c7d8e9f0
النسخة #2: b4c3d2e1f0a9b8c7d6e5f4a3b2c1d0e9f8a7b6c5d4e3f2a1b0c9d8e7f6a5b4
```

**المزايا:**
- كشف التلاعب بالملفات
- التحقق من سلامة البيانات
- تاريخ التغييرات الكامل

### Auto-Sync — المزامنة التلقائية

```
1. فعّل "✅ مزامنة تلقائية"
2. حدد الفئة الزمنية:
   - كل 5 دقائق
   - كل 15 دقيقة
   - كل ساعة
   - يومياً
3. عند تعديل أي ملف → يُرفع تلقائياً
4. يمكنك إيقاف المزامنة مؤقتاً
```

---

## 12. الإعدادات المتقدمة {#advanced-features}

### Analytics — التحليلات

#### الإحصائيات المتاحة

| الإحصائية | الوصف |
|-----------|-------|
| **عدد المحادثات** | إجمالي محادثات AI |
| **عدد الطلبات** | الطلبات إلى المزود |
| **متوسط الاستجابة** | متوسط وقت الرد |
| **الاستخدام اليومي** | استخدام كل يوم |
| **الاستخدام الأسبوعي** | ملخص الأسبوع |
| **الاستخدام الشهري** | ملخص الشهر |
| **أكثر الملفات تعديلاً** | الملفات الأكثر تغييراً |
| **أكثر الأدوات استخداماً** | الأدوات الأكثر استخداماً |

#### تقارير يومية/أسبوعية/شهرية

```
📊 Analytics
├── 📅 يومي
│   ├── عدد الطلبات
│   ├──Peak Hour (ساعة الذروة)
│   └── متوسط مدة المحادثة
├── 📆 أسبوعي
│   ├── Daily Breakdown
│   ├── Top Items
│   └── Top Types
└── 📆 شهري
    ├── Growth (النمو)
    ├── Most Active Day
    └── Weekly Breakdown
```

### Calendar — التقويم

```
📅 Calendar
├── عرض الطلبات حسب التاريخ
├── تحديد نطاق زمني محدد
├── تصدير التقويم
└── مقارنة فترات مختلفة
```

### Reports — التقارير

#### أنواع التقارير

| التقرير | المحتوى |
|---------|---------|
| **تقرير الاستخدام** | تفصيل استخدام كل ميزة |
| **تقرير الأداء** | أوقات الاستجابة والأخطاء |
| **تقرير الأمان** | محاولات الوصول وال Threats |
| **تقرير التكلفة** | تكلفة استخدام المزودين |
| **تقرير مخصص** | تقرير بمعايير حرة |

#### تصدير التقارير

```
📥 التصدير
├── PDF (تنزيل)
├── HTML (عرض في المتصفح)
├── JSON (للمعالجة)
└── CSV (لـ Excel)
```

### Security — الأمان

#### إعدادات التشفير

| الإعداد | الوصف | القيمة الافتراضية |
|---------|-------|-------------------|
| **خوارزمية التشفير** | خوارزمية التشفير | AES-GCM |
| **طول المفتاح** | طول مفتاح التشفير | 256 بت |
| **تشفير التخزين** | تشفير البيانات المخزنة | ✅ |
| **تشفير النقل** | تشفير البيانات المنقولة | ✅ |

#### سجل الأمان

```
🔒 Security Log
├── محاولات الدخول
├── طلبات غير مصرح بها
├── تغييرات الإعدادات
└── أنشطة مشبوهة
```

### Collaboration — التعاون

#### إدارة المستخدمين

```
👥 Collaborators
├── 👤 Admin (مسؤول)
│   └── صلاحيات كاملة
├── 👤 Editor (محرر)
│   └── تعديل المحتوى فقط
├── 👤 Viewer (مشاهد)
│   └── قراءة فقط
└── 👤 Guest (ضيف)
    └── وصول محدود
```

#### سجل التغييرات المشتركة

```
📋 Changelog
├── من قام بالتغيير
├── متى تم التغيير
├── ما الذي تم تغييره
└── مراجعة التغييرات
```

---

## حل المشاكل {#troubleshooting}

### مشاكل شائعة

#### "خطأ 429: Rate limit exceeded"
```
السبب: تجاوزت حد الطلبات المجاني
الحل:
1. انتظر حتى يتجدد العدد (كل 24 ساعة)
2. استخدم مزوداً آخر مؤقتاً
3. اشحن حسابك للحصول على المزيد
```

#### "خطأ 404: model not found"
```
السبب: نموذج غير متاح أو مُغلق
الحل:
1. تأكد من اختيار نموذج نشط (gemini-3.x)
2. لا تستخدم نماذج مُغلقة (gemini-2.0, 1.5)
3. راجع جدول النماذج المتاحة
```

#### "البحث لا يُرجع نتائج"
```
السبب: لا توجد نتائج مطابقة
الحل:
1. جرّب كلمات مختلفة
2. استخدم البحث الضبابي (fuzzy)
3. جرّب البحث بالإنجليزية
```

#### "GitHub token غير صالح"
```
الحل:
1. تأكد من صحة التوكن
2. تأكد من الصلاحيات (repo)
3. أنشئ توكن جديد إذا لزم الأمر
```

---

## 13. نظام الأدوات المتعدد {#multi-tool-system}

### نظرة عامة

نظام الأدوات المتعدد يتيح تنفيذ المهام المعقدة باستخدام أدوات متعددة مع تراجع تلقائي عند الفشل. يدعم Aider (أولوية)، Cline (بديل)، وAgent مخصص (متاح دائماً).

### الأدوات المدعومة

| الأداة | الأولوية | الوصف | طريقة التثبيت |
|--------|---------|-------|---------------|
| **Aider** | 1 (أولوية) | أداة CLI للبرمجة بالذكاء الاصطناعي | `pip install aider-chat` |
| **Cline** | 2 (بديل) | أداة CLI بديلة للبرمجة | `npm install -g @anthropic-ai/cline` |
| **Agent مخصص** | 3 (احتياطي) | يستخدم مزودي AI مباشرة | متاح دائماً |

### آلية العمل

```
المستخدم ← يكتب مهمة ← AI Provider يخطط ← Local Agent ينفذ ← يعرض النتائج
                                    │
                    ┌───────────────┼───────────────┐
                    ▼               ▼               ▼
               Aider (1)      Cline (2)      Agent مخصص (3)
                    │               │               │
                    └───────────────┴───────────────┘
                                    │
                              تراجع تلقائي عند الفشل
```

### إعدادات الأدوات

يمكنك اختيار أداة محددة أو استخدام التراجع التلقائي:

- **تلقائي (الافتراضي):** Aider → Cline → Agent مخصص
- **Aider فقط:** تنفيذ عبر Aider بدون تراجع
- **Cline فقط:** تنفيذ عبر Cline بدون تراجع
- **Agent مخصص فقط:** تنفيذ عبر مزودي AI مباشرة

### استخدام نظام الأدوات

#### عبر WebSocket

```javascript
// إرسال مهمة للتنفيذ
ws.send(JSON.stringify({
  id: 'task-001',
  type: 'task',
  payload: {
    task: {
      goal: 'أنشئ ملف index.html بسيط',
      type: 'general',
    },
    settings: {
      selectedTool: 'aider',  // أو null للتراجع التلقائي
      autoFallback: true,
    }
  }
}));

// فحص حالة الأدوات
ws.send(JSON.stringify({
  id: 'status-001',
  type: 'tool-status',
  payload: {}
}));
```

---

## 14. مقدمات AI للوكيل المحلي {#agent-ai-providers}

### نظرة عامة

الوكيل المحلي (CyberGuard Agent) يدعم 5 مقدمات AI مع واجهة موحدة و FALLBACK تلقائي. هذا يتيح استخدام مزودين مختلفين للحصول على أفضل أداء وتوفر.

### المزودات المدعومة

| المزود | الميزة الرئيسية | المفتاح | الحد المجاني | هل يحتاج إنترنت؟ |
|--------|-----------------|---------|--------------|------------------|
| **Gemini** | المزود الافتراضي | `GEMINI_API_KEY` | 1500 طلب/يوم | نعم |
| **Ollama** | يعمل محلياً | لا يحتاج مفتاح | غير محدود | لا |
| **Groq** | سرعة عالية | `GROQ_API_KEY` | 30 طلب/دقيقة | نعم |
| **HuggingFace** | موديلات متنوعة | `HUGGINGFACE_API_KEY` | 1000 طلب/يوم | نعم |
| **OpenRouter** | وصول لموديلات متعددة | `OPENROUTER_API_KEY` | حسب الموديل | نعم |

### إعداد المفاتيح

#### إعداد مفاتيح API (اختياري)

```bash
# إعداد المفاتيح كمتغيرات بيئة
export GEMINI_API_KEY="AIzaSy..."      # المزود الافتراضي
export GROQ_API_KEY="gsk_..."          # سرعة عالية
export HUGGINGFACE_API_KEY="hf_..."    # موديلات متنوعة
export OPENROUTER_API_KEY="sk-or-..."  # وصول لموديلات متعددة

# Ollama يعمل محلياً — لا يحتاج مفتاح
# تأكد من تشغيل Ollama أولاً
ollama serve
```

#### إعداد المفاتيح في الوكيل

```bash
# يمكنك أيضاً تمرير المفاتيح عند تشغيل الوكيل
GEMINI_API_KEY="your-key" cyberguard-agent start --port 3002
```

### استخدام المقدمات

#### عبر WebSocket

```javascript
// إرسال رسالة AI عبر مزود محدد
ws.send(JSON.stringify({
  id: 'ai-001',
  type: 'ai-chat',
  payload: {
    message: 'اشرح لي كيف يعمل SQL injection',
    provider: 'gemini'  // أو 'ollama', 'groq', 'huggingface', 'openrouter'
  }
}));

// استجابة
{
  "id": "ai-001",
  "status": "complete",
  "result": {
    "response": "SQL injection هو...",
    "provider": "gemini",
    "model": "gemini-3.5-flash"
  }
}
```

#### سرد المزودات المتاحة

```javascript
// سرد جميع المزودات المتاحة
ws.send(JSON.stringify({
  id: 'providers-001',
  type: 'ai-providers',
  payload: {}
}));

// استجابة
{
  "id": "providers-001",
  "status": "complete",
  "result": {
    "providers": [
      { "id": "gemini", "name": "Google Gemini", "available": true },
      { "id": "ollama", "name": "Ollama (Local)", "available": true },
      { "id": "groq", "name": "Groq", "available": false },
      { "id": "huggingface", "name": "HuggingFace", "available": false },
      { "id": "openrouter", "name": "OpenRouter", "available": false }
    ],
    "active": "gemini"
  }
}
```

### الفصل التلقائي (Fallback)

إذا فشل المزود الحالي، يحاول المزود التالي تلقائياً:

```
1. المزود النشط (Gemini افتراضياً)
   ↓ (فشل)
2. المزودات الأخرى حسب التوفر
   ↓ (فشل)
3. رسالة خطأ
```

**مثال:**
- Gemini يتجاوز الحد → يحاول Groq
- Groq غير متاح → يحاول Ollama
- Ollama لا يعمل → رسالة خطأ

### إعداد Ollama المحلي

#### التثبيت

```bash
# Linux
curl -fsSL https://ollama.ai/install.sh | sh

# macOS
brew install ollama

# Windows
# حمّل من https://ollama.ai
```

#### تشغيل الخادم

```bash
# تشغيل الخادم
ollama serve

# تحميل موديل
ollama pull llama3

# اختبار
ollama run llama3 "Hello, how are you?"
```

#### إعداد في الوكيل

```bash
# Ollama يعمل على المنفذ 11434 افتراضياً
# لا يحتاج مفتاح API
# فقط اختر 'ollama' كمزود في الوكيل
```

### مقارنة المزودات

| المعيار | Gemini | Ollama | Groq | HuggingFace | OpenRouter |
|---------|--------|--------|------|-------------|------------|
| **التكلفة** | مجاني | مجاني | مجاني جزئياً | مجاني | مجاني جزئياً |
| **السرعة** | عالية | حسب الجهاز | عالية جداً | متوسطة | حسب الموديل |
| **الخصوصية** | بيانات تذهب للسحابة | محلي بالكامل | بيانات تذهب للسحابة | بيانات تذهب للسحابة | بيانات تذهب للسحابة |
| **جودة الموديل** | عالية | حسب الموديل | عالية | متنوعة | متنوعة |
| **الإنترنت** | مطلوب | غير مطلوب | مطلوب | مطلوب | مطلوب |
| **الحد المجاني** | 1500/يوم | غير محدود | 30/دقيقة | 1000/يوم | حسب الموديل |

### نصائح للاختيار

1. **للتعلم اليومي:** Gemini (مجاني، سريع، جودة عالية)
2. **للخصوصية:** Ollama (محلي بالكامل، لا إنترنت)
3. **للسرعة:** Groq (أسرع برمجياً)
4. **لموديلات متنوعة:** HuggingFace أو OpenRouter
5. **للاختبار المحلي:** Ollama (بدون تكلفة)

### استكشاف الأخطاء

#### "Gemini API key غير صالح"
```
الحل:
1. تأكد من صحة المفتاح (يبدأ بـ AIza)
2. تأكد من تفعيل Google AI Studio API
3. أنشئ مفتاح جديد من aistudio.google.com
```

#### "Ollama غير متاح"
```
الحل:
1. تأكد من تشغيل 'ollama serve'
2. تأكد من المنفذ 11434
3. أعد تشغيل الخادم
```

#### "Groq rate limit exceeded"
```
الحل:
1. انتظر حتى يتجدد العدد (كل دقيقة)
2. استخدم مزوداً آخر مؤقتاً
3. قلل عدد الطلبات
```

---

## ملاحظات مهمة

- **🔒 أمان:** لا تشارك مفاتيح API مع أحد
- **💰 التكلفة:** Gemini المجاني كافي للاستخدام العادي
- **📊 المراقبة:** راجع Analytics بانتظام
- **💾 النسخ الاحتياطي:** فعّل المزامنة التلقائية
- **🔄 التحديثات:** حدّث المزودين بانتظام
- **🤖 الوكيل:** استخدم Ollama للخصوصية الكاملة

---

## مشاكل معروفة (Known Bugs)

> آخر تدقيق: 2026-06-23 — التقرير الكامل في `AUDIT_REPORT.md`

### مقدمات الوكيل المحلي (Agent AI Providers)

|المشكلة | المزود | التأثير | الحل المقترح |
|--------|--------|---------|-------------|
| **HuggingFace استجابة خاطئة** | HuggingFace | المزود غير وظيفي — يسترجع `generated_text` من نص | إصلاح تحليل الاستجابة |
| **حالة AI Fallback** | جميع المزودات | الطلبات المتزامنة تتداخل | استخدام متغير محلي |
| **OpenCode أزرار المزود** | OpenCode | أزرار المزود وهمية | إضافة حالة `opencodeProvider` |

### ملاحظات

- **HuggingFace** — غير وظيفي حالياً — استخدم Gemini أو Ollama كبديل
- **Gemini** — المزود الأفضل — مجاني حتى 1500 طلب/يوم
- **Ollama** — الأفضل للخصوصية — يعمل محلياً بدون إنترنت
- **OpenCode CLI `run`** — يواجه مشاكل في v1.15.10 — استخدم Desktop app

---

> **آخر تحديث:** 2026-06-23
> **الإصدار:** 10.1.0
> **المطور:** YoussefAhamedKamal
